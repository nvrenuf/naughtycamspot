---
export { getStaticPaths } from '../../v2/blog/[slug].astro';

const { slug } = Astro.params;
const target = `/v2/blog/${slug}`;

export const prerender = true;
return Astro.redirect(target, 308);
---
